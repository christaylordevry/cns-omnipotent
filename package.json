{
  "name": "cns",
  "version": "0.0.0",
  "private": true,
  "description": "CNS Phase 1: vault contract, constitution mirror, Vault IO (scaffold grows in later stories)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
