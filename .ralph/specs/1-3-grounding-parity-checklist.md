---
story_key: 1-3-grounding-parity-checklist
source_story: _bmad-output/implementation-artifacts/1-3-grounding-parity-checklist.md
generated_at: 2026-04-02T06:02:11Z
completion_promise: LOOP_COMPLETE
---

# Ralph spec: Story 1.3: Grounding parity checklist

Generated by `scripts/bmad_to_ralph.py`. Re-run after editing the BMAD story.

## Source

BMAD story: `_bmad-output/implementation-artifacts/1-3-grounding-parity-checklist.md`

## Success criteria

Stop the Ralph loop when `bash scripts/verify.sh` passes and every acceptance criterion in the story is done.

## Acceptance criteria (from story)

1. **Operator-facing doc:** README or operator doc (repo or vault-side path already used in Epic 1) contains a **checklist** covering: one-time MCP configuration for **Cursor** and **Claude Code** on **WSL**, and the steps that align with Story 1.1–1.2 outcomes (constitution load without manual paste, vault-relative paths). No duplicate constitution paste steps beyond what one-time MCP setup requires.

2. **Parity (NFR-I1):** The checklist explicitly states that **both** surfaces follow the same **grounding** and **Vault IO–mediated** journey expectations after one-time setup, and calls out any intentional deltas (if any) in one short subsection.

3. **Time-to-grounded (NFR-P1):** The doc defines a **repeatable observation method** for time-to-grounded spot checks: what to start the stopwatch on (e.g. workspace open or first agent message), what counts as “task-ready” (constitution + MCP tools usable without routing paste), and a simple note that the **median** target is **under 30 seconds** per `prd.md` NFR-P1 (no full benchmark harness required in Phase 1).

4. **Verification gate:** `bash scripts/verify.sh` passes; if the checklist lives in markdown under `specs/` or `_bmad-output/`, add or extend a **lightweight automated check** (e.g. Node test) that required section headings or keywords exist so the checklist cannot silently rot.

## Tasks / subtasks (from story)

- [ ] **Choose doc home** (AC: #1)  
  - [ ] Extend an existing operator-facing README (prefer `specs/cns-vault-contract/README.md`, `_bmad-output/planning-artifacts/cns-vault-contract/README.md`, or `CLAUDE.md` project pointer) rather than sprawl; one primary checklist location.

- [ ] **Write checklist body** (AC: #1, #2, #3)  
  - [ ] Cursor: one-time steps (MCP, workspace at vault root, rules if any).  
  - [ ] Claude Code on WSL: one-time steps (MCP, vault root, shim from `specs/cns-vault-contract/shims/` as needed).  
  - [ ] NFR-I1 parity subsection; NFR-P1 observation rubric (start event, done condition, 30s median reference).

- [ ] **Automated guard** (AC: #4)  
  - [ ] Add or extend `tests/*.test.mjs` to assert the checklist file(s) contain expected anchors (titles or keywords).  
  - [ ] Run `bash scripts/verify.sh` until green.

## Operator hints

- `ralph.yml` should set `core.specs_dir` to `.ralph/specs/` (see repo root).
- Use `PROMPT.md` or explicit `ralph run` instructions for the active story.
