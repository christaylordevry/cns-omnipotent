---
story_key: 2-1-per-directory-manifests-for-the-folder-contract
source_story: _bmad-output/implementation-artifacts/2-1-per-directory-manifests-for-the-folder-contract.md
generated_at: 2026-04-02T06:48:58Z
completion_promise: LOOP_COMPLETE
---

# Ralph spec: Story 2.1: Per-directory manifests for the folder contract

Generated by `scripts/bmad_to_ralph.py`. Re-run after editing the BMAD story.

## Source

BMAD story: `_bmad-output/implementation-artifacts/2-1-per-directory-manifests-for-the-folder-contract.md`

## Success criteria

Stop the Ralph loop when `bash scripts/verify.sh` passes and every acceptance criterion in the story is done.

## Acceptance criteria (from story)

1. **Contract-derived manifests:**  
   Given the Phase 1 folder contract from `specs/cns-vault-contract/CNS-Phase-1-Spec.md`,  
   when `_README.md` manifests are created for each governed directory,  
   then each manifest includes purpose, schema requirements (where relevant), allowed PAKE types, and naming_convention in an agent-readable way. (FR4, FR5)

2. **Routing-aligned type policy:**  
   Given the routing rules in `specs/cns-vault-contract/AGENTS.md` (Vault Map + Routing Rules),  
   when manifests describe where each PAKE note type goes by default,  
   then the allowed_pake_types and “what goes here” guidance are consistent with Vault IO placement expectations (outside Inbox). (FR5)

3. **Spec parity gap tracking:**  
   Given the manifests are authored,  
   when you review gaps against `CNS-Phase-1-Spec.md` directory contract obligations,  
   then any missing/ambiguous elements are explicitly called out for follow-up so future refinement does not silently drift from FR4. (FR4)

## Tasks / subtasks (from story)

- [ ] Create contract `_README.md` manifests (agent-readable)
  - [ ] `00-Inbox/_README.md`
    - [ ] `schema_required: false`
    - [ ] `allowed_pake_types: any`
    - [ ] `naming_convention: any Obsidian-friendly .md name; triage later applies PAKE standard when moved out of Inbox`
    - [ ] Document Inbox exception: raw capture, triage destination, and “no schema enforcement on initial create” expectation.
    - [ ] Document `Frontmatter Requirements` section: initial Inbox captures may omit YAML; after triage, notes must receive PAKE standard frontmatter.
  - [ ] `01-Projects/_README.md`
    - [ ] `schema_required: true`
    - [ ] `allowed_pake_types: WorkflowNote` (route by project context expectations)
    - [ ] `naming_convention: project-scoped, human-readable filenames; no directory-scoped schema encoded in the filename`
    - [ ] Document directory role (“Active project folders”) and “what goes here” vs “what does not go here”.
    - [ ] Document `Frontmatter Requirements` section: notes outside `00-Inbox/` must include PAKE standard frontmatter.
  - [ ] `02-Areas/_README.md`
    - [ ] `schema_required: true`
    - [ ] `allowed_pake_types: WorkflowNote`
    - [ ] `naming_convention: descriptive names aligned to ongoing responsibilities; avoid date-specific prefixes here`
    - [ ] Document ongoing responsibilities semantics and routing expectations.
    - [ ] Document `Frontmatter Requirements` section: notes outside `00-Inbox/` must include PAKE standard frontmatter.
  - [ ] `03-Resources/_README.md`
    - [ ] `schema_required: true`
    - [ ] `allowed_pake_types: SourceNote | InsightNote | SynthesisNote | ValidationNote`
    - [ ] `naming_convention: reference- and concept-scoped names; stable titles so agents can search reliably`
    - [ ] Document reference/knowledge semantics and “gatekeeping” rules (no untriaged Inbox content).
    - [ ] Document `Frontmatter Requirements` section: notes outside `00-Inbox/` must include PAKE standard frontmatter.
  - [ ] `04-Archives/_README.md`
    - [ ] `schema_required: true`
    - [ ] `allowed_pake_types: any` with guidance that notes should be treated as archived content.
    - [ ] `naming_convention: human-readable archived titles; status frontmatter should be `archived``
    - [ ] Document read-only intent (“read-only unless reactivating”) and naming expectations for archived items.
    - [ ] Document `Frontmatter Requirements` section: notes outside `00-Inbox/` must include PAKE standard frontmatter.
  - [ ] `DailyNotes/_README.md`
    - [ ] `schema_required: true`
    - [ ] `allowed_pake_types: WorkflowNote`
    - [ ] `naming_convention: DailyNotes/YYYY-MM-DD.md`
    - [ ] Document daily file naming convention `YYYY-MM-DD.md` and append-only during the day, reviewed weekly expectations.
    - [ ] Document `Frontmatter Requirements` section: notes outside `00-Inbox/` must include PAKE standard frontmatter (with daily tags).

- [ ] Verify manifests conform to the required contract template
  - [ ] Every manifest has YAML frontmatter fields: `purpose`, `schema_required`, `allowed_pake_types`, `naming_convention`.  
  - [ ] Every manifest body contains: directory name heading, “What Goes Here” and “What Does Not Go Here”, and “Frontmatter Requirements”.
  - [ ] No prose violates vault formatting constraints from `specs/cns-vault-contract/AGENTS.md` (notably no em dash usage).

- [ ] Align manifests with PAKE frontmatter expectations
  - [ ] For directories with `schema_required: true`, document that notes outside `00-Inbox/` must include the PAKE standard frontmatter block.
  - [ ] For `00-Inbox/`, document that notes can be created without PAKE frontmatter initially, and that triage applies schema when moved out of Inbox.

- [ ] Document the deploy target and where operator should copy/sync from
  - [ ] If the repository does not include a mock `Knowledge-Vault-ACTIVE/` directory, explicitly state the vault deploy locations so the manifests can be installed on the operator machine.

## Operator hints

- `ralph.yml` should set `core.specs_dir` to `.ralph/specs/` (see repo root).
- Use `PROMPT.md` or explicit `ralph run` instructions for the active story.
