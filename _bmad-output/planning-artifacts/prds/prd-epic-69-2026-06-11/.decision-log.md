# Decision Log — PRD Epic 69

## 2026-06-11 — Run opened

- **Intent:** Create (operator supplied full Epic 69 brief)
- **Working mode:** Fast path — operator provided scope, constraints, and five feature areas; draft with `[ASSUMPTION]` tags where inferred
- **Workspace:** `_bmad-output/planning-artifacts/prds/prd-epic-69-2026-06-11/`
- **Stake calibration:** Internal operator tool (CNS Nexus); rigor matches Epics 66–68 PRDs

## 2026-06-11 — Scope decisions

| Decision | Rationale |
|----------|-----------|
| Story order prioritizes **operator daily triage** over **Screen 10 build** | Disposition feed + dedup/people inspector fixes are used every digest; investigation board is session-spanning but less frequent |
| Source health requires **digestRun metadata extension** (not new adapters) | Signals alone cannot distinguish "unavailable" from "no matches"; optional `sourceOutcomes[]` on push is minimal compute touch, allowed under "Convex queries extending" |
| People match **person name** requires **push metadata** or dashboard-sync of people list | `nexus-people.yaml` lives on operator machine; Vercel UI cannot read `~/.hermes/` — FR-3 includes optional `sourceMetadata.peopleMatch` from scoring push |
| Digest signal feed is **new UI surface** (`NexusDigestSignalFeed`) | No ranked digest feed exists on `/nexus` today; disposition hierarchy applies there, not Signal Seeds rail (keyword candidates) |
| Investigation workspace uses **new `investigationBoardItems` table** | `investigationSessions` is per-action AI runs, not kanban membership; Screen 10 needs board state separate from session streaming |

## 2026-06-11 — Finalized

- PRD status set to `final`
- Reviewer gate: skipped (operator-requested deliverable run; no ad-hoc review requested)
- Next recommended: `/bmad-create-architecture` then `/bmad-create-story` for 69-1
