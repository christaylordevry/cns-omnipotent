# Decision Log — Epic 67 PRD

## Session 2026-06-09

- **Intent:** Create PRD for Epic 67 — Signal Quality + Source Expansion.
- **Working mode:** Fast path (operator brief supplied full scope, success criteria, constraints, and out-of-scope list).
- **Stakes:** Internal operator tool — rigor calibrated to Epic 64–66 PRD depth; story map for `/bmad-create-story`.

## Decisions

| # | Decision | PRD location |
|---|----------|--------------|
| 1 | Epic 67 is **validation + scoring + UX wiring + one new adapter** — not a new scoring engine or Screen 10 | §1, §5 |
| 2 | `last30days-skill-reference` remains codebook-only per ADR-E67-001; ProductHunt adapter is native Node/TS | §3, §12, addendum A1 |
| 3 | `nexus-goals.yaml` lives at `~/.hermes/nexus-goals.yaml`; goal phrases weighted **2×** sprint tokens in `scorePersonalRelevance` | §4.3, addendum A2 |
| 4 | Signal Seeds chip click resolves `topicSlug` via `displayTerm` → trend topic keyword match (65-9 T0 deferred work) | §4.4 |
| 5 | Compare smoke test is **operator-validated documentation** story, not automated CI — requires ≥2 digest runs with same topic | §4.6 |
| 6 | GitHub/RSS live validation is **gate** for declaring Epic 65 adapters production-ready | §13 story map |
| 7 | Reddit credential provisioning is **operator prerequisite**; story 67-2 wires env + validates live run | §4.2 |
| 8 | ProductHunt = Source 10; extends `digestSourceTypeValue` with `producthunt` literal | §4.5 |
| 9 | FR IDs scoped to Epic 67 (FR-1..FR-N); no cross-epic FR continuation | §4 |
| 10 | Screen 10, X/Twitter, LinkedIn, multi-tenant `workspaceId` — explicit non-goals per operator brief | §5, §6.2 |

## Overrides / assumptions

- Google Trends watchlist already updated to 14 topic-aligned keywords (operator session) — no FR unless validation fails.
- `personalRelevance` env fix (symlink + `MORNING_DIGEST_PROJECT_ENTITIES`) landed this session — v2 adds goals file on top.
- `NEXUS_COMPARE_ENABLED=true` already set — smoke test can proceed once digest runs exist.

## Deferred to architecture

- ProductHunt API endpoint choice (GraphQL vs REST) — addendum A1 options; architecture picks one.
- Exact `nexus-goals.yaml` schema validation rules — addendum A2 draft; architecture finalizes.
- Reddit OAuth app retry outcome — operator-dependent; 67-2 documents GO/NO-GO.

## Finalization

- **Status:** final
- **Date:** 2026-06-09
- **Next workflows:** `/bmad-create-architecture`, `/bmad-create-epics-and-stories`, `/bmad-sprint-planning`
