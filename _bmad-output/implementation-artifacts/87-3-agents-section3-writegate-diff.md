# Story 87-3 — Proposed AGENTS.md §3 WriteGate diff (AC8)

**Baseline:** `/mnt/c/Users/Christopher Taylor/Knowledge-Vault-ACTIVE/AI-Context/AGENTS.md` v2.1.52 (2026-07-09)  
**Target:** v2.1.53  
**Apply via:** operator `/session-close` WriteGate only — do NOT direct-edit vault AGENTS in IDE.

---

## Unified diff (§3 Frontmatter Template block)

```diff
--- a/AI-Context/AGENTS.md
+++ b/AI-Context/AGENTS.md
@@ -1,6 +1,6 @@
 # AGENTS.md - Central Nervous System Constitution
 
-> Version: 2.1.52 | Last updated: 2026-07-09
+> Version: 2.1.53 | Last updated: 2026-07-09
 > Canonical vault path: `Knowledge-Vault-ACTIVE/AI-Context/AGENTS.md`  
 > Git mirror (implementation repo): `../../specs/cns-vault-contract/AGENTS.md` (relative from this `AI-Context/` folder when the vault lives under `Knowledge-Vault-ACTIVE/` in the Omnipotent.md clone).
 
@@ -114,15 +114,24 @@ pake_type: [SourceNote | InsightNote | SynthesisNote | WorkflowNote | Validation
 title: "[Human-readable title]"
 created: [YYYY-MM-DD]
 modified: [YYYY-MM-DD]
 status: [draft | in-progress | reviewed | archived]
-confidence_score: [0.0 to 1.0]
-verification_status: [pending | verified | disputed]
-creation_method: [human | ai | hybrid]
 tags:
   - [relevant tags]
 ---
 
+### Optional Quality Enrichment
+
+These fields are part of the PAKE quality-enrichment tier. They are **optional** on governed notes; absent fields must not block `vault_move` or `vault_update_frontmatter` triage.
+
+- **Hermes Vault IO** (`vault_create_note`, `vault_append_daily`) stamps defaults: `confidence_score: 0.5`, `verification_status: pending`, `creation_method: ai`.
+- **`/verify`** (Epic 30) may update `verification_status` and `modified` when the operator confirms a note.
+- **Nexus** direct-FS captures may omit enrichment until triaged into canonical PAKE shape.
+
+```yaml
+confidence_score: [0.0 to 1.0]
+verification_status: [pending | verified | disputed]
+creation_method: [human | ai | hybrid]
+```
+
 Optional fields (use when applicable):
 
 ```yaml
@@ -428,6 +437,7 @@ When speaking to the USER about which model you selected for a subagent, do NOT
 
 | Date | Version | Change |
 |------|---------|--------|
+| 2026-07-09 | 2.1.53 | **§3:** Quality-enrichment tier (`confidence_score`, `verification_status`, `creation_method`) moved from required template to optional subsection; Hermes defaults and Nexus triage guidance (Story 87-3). |
 | 2026-07-09 | 2.1.52 | **Section 8:** Regenerated by /session-close after story 87-2; Epic 87 in-progress. |
```

---

## Post-apply parity

After session-close WriteGate applies this patch to the canonical vault copy, sync identical bytes to:

1. `specs/cns-vault-contract/AGENTS.md` (repo mirror)
2. Planning mirror per `constitution.test.mjs` parity rules

`constitution.test.mjs` must pass with all copies byte-identical.
