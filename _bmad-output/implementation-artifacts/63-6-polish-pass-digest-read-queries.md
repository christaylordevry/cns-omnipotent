# Story 63.6: Polish pass + digest read queries + route redirect

Status: ready-for-dev

**Implementation repo:** `../cns-dashboard` — canonical story with full dev context lives at  
`cns-dashboard/_bmad-output/implementation-artifacts/63-6-polish-pass-digest-read-queries.md`

## Summary

Final Epic 63 story. Adds Convex digest read queries, wires inspector "Why This Matters", drawer a11y (escape, focus trap), clickable related signals, reduced-motion audit, and `/` → `/nexus` redirect. Verify via `bash scripts/verify.sh`.

See sibling repo story file for complete acceptance criteria, tasks, and dev notes.
