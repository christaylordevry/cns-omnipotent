# Knowledge-Vault-ACTIVE

Deployable mock / active vault tree for CNS Phase 1; constitution and modules under `AI-Context/`.

**Standalone vault:** If you open only this folder in Obsidian and not as `…/Omnipotent.md/Knowledge-Vault-ACTIVE/`, the `../../` and `../../../` links in `AI-Context/AGENTS.md` and `AI-Context/modules/*.md` will not resolve—use the implementation repo’s `specs/cns-vault-contract/` mirror or open the full clone.
