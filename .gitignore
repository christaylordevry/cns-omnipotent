# secrets
.env
.env.*
*.pem
*.key

# ralph runtime noise (keep specs/tasks, ignore logs)
.ralph/diagnostics/
.ralph/events.jsonl
.ralph/loop.lock
.ralph/**/loop.lock

# local Claude worktrees
.claude/worktrees/

# generated export bundles (NotebookLM / operator artifacts)
scripts/output/

# Hermes morning digest Mode B inbox captures (operator-local; Story 26-7)
Knowledge-Vault-ACTIVE/00-Inbox/hermes-morning-digest-*.md

# node / python common
node_modules/
dist/
build/
__pycache__/
.venv/
.pytest_cache/
